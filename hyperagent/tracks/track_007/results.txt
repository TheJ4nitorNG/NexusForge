# Track 007 Telemetry Results

- **Efficiency**: Medium-High (~20 turns for implementing three complex engines and their full integration test suites).
- **Fidelity**: High (Zero placeholders, 100% test pass rate, exact adherence to production standards).
- **Errors Encountered**:
  - `IDE0005`: Still struggling with unnecessary `using` directives generated by `dotnet new` or leftover from refactoring.
  - `CA1816`: Missed `GC.SuppressFinalize(this)` in the test class implementing `IDisposable`.
  - `CS1061`: Test mocking error using `Throws` instead of `ThrowsAsync` for asynchronous task returning methods in NSubstitute.
- **User Feedback**: "4/5 again, struggled with some tests, but got back on track. please stop wasting my tokens on failed tests"
- **Metacognitive Insight**:
  1. The user is highly sensitive to token waste caused by trial-and-error build/test loops.
  2. To eliminate test/build failures on the first pass, the agent MUST rely heavily on `dotnet format` immediately after writing code to clear out `IDE0005` automatically, rather than doing build-fail-replace loops.
  3. When writing asynchronous tests with `NSubstitute`, use `.ThrowsAsync` for `Task` returning methods.
  4. Always add `GC.SuppressFinalize(this)` when implementing `IDisposable`.